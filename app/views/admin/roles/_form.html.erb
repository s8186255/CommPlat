<%= simple_form_for [:admin, @role] ,html: {class: 'form-horizontal'},  defaults: { } do |f| %>
  <%= f.input :name, label: '角色名' %>
  <%= f.input :desc, label: '描述' %>
  <div class="line line-dashed b-b line-lg pull-in"></div>
  <div class="col-sm-12 col-sm-offset-2">
    <%= link_to  admin_roles_path, class: "btn btn-default" do%>
      返 回
    <% end %>
    <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
      保 存
    <% end %>
  </div>
<% end %>
